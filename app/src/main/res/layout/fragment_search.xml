<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.search.SearchFragment">

        <TextView
            android:id="@+id/fragment_search_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/search"
            android:background="@color/white"
            android:textColor="@color/colorError"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:textAllCaps="true"
            android:textSize="32sp" />

        <com.google.android.material.search.SearchBar
            android:id="@+id/fragment_search_searchBar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/fragment_search_searchBtn"
            app:layout_constraintTop_toBottomOf="@id/fragment_search_title"/>

        <ImageButton
            android:id="@+id/fragment_search_searchBtn"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:background="@color/colorError"
            app:layout_constraintTop_toTopOf="@id/fragment_search_searchBar"
            app:layout_constraintBottom_toBottomOf="@id/fragment_search_searchBar"
            android:src="@drawable/check_icon"
            android:layout_marginEnd="16dp"
            app:layout_constraintEnd_toEndOf="parent"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/fragment_search_rv"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="16dp"
            android:layout_marginHorizontal="16dp"
            app:layout_constraintTop_toBottomOf="@id/fragment_search_searchBar"
            app:layout_constraintBottom_toBottomOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
